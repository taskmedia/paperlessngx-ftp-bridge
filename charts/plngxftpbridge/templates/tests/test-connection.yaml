apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plngxftpbridge.fullname" . }}-test-connection"
  labels:
    {{- include "plngxftpbridge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plngxftpbridge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
